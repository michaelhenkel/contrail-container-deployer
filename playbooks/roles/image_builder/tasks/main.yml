---
- name: get centos image
  include: get_centos_image.yml

- name: build and start container vms
  include: build_and_start_container_vms.yml
  with_dict: "{{ hostvars[inventory_hostname].container_vms }}"
